# TODO: Fix Order Status Update Issues

## Tasks:
- [ ] 1. Fix VendorOrders.tsx - Change `orderStatus.toUpperCase()` to `orderStatus` in desktop select
- [ ] 2. Fix VendorOrders.tsx - Change `orderStatus.toUpperCase()` to `orderStatus` in mobile select
- [ ] 3. Fix VendorOrders.tsx - Remove unused `updateOrderStatus` function
- [ ] 4. Fix update-status API route.ts - Add handling for "cancelled" and "returned" statuses
- [ ] 5. Verify the fixes

